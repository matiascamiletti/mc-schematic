{
  "$schema": "http://json-schema.org/schema",
  "$id": "MCKitSchematicsAddTable",
  "title": "Generate a new table",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Ruta de la entidad"
    },
    "component": {
      "type": "string",
      "description": "Ruta donde se creara el component"
    }
  },
  "required": ["name", "component"]
}
  